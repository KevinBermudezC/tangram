<!--
Thanks for contributing to Tangram.

Pick one of the three change types below and fill in the sections that apply.
Delete any section that doesn't fit your PR. The template is a guide, not a
contract — keep it short.

For non-trivial changes, an OpenSpec proposal should land first as its own PR.
See CONTRIBUTING.md for when a proposal is needed.
-->

## Type of change

<!-- Tick the box that applies — leave the rest. Add a custom one if none fit. -->

- [ ] **Proposal + implementation** — proposal under `openspec/changes/<change-id>/` and the code that implements it, together (default for most non-trivial changes)
- [ ] **Proposal only** — proposal under `openspec/changes/<change-id>/`, no implementation yet (use when the approach needs feedback before coding)
- [ ] **Implementation only** — implements an already-merged proposal
- [ ] **Archive** — moves a completed proposal's specs into `openspec/specs/` after the implementation merged
- [ ] **Trivial** — typo, dependency bump, small bug fix, doc tweak (no proposal needed)

## Summary

<!-- 1–3 bullet points. What does this PR do? -->

-
-

## Linked artifacts

<!-- Replace with the relevant paths or remove the row if none. -->

- OpenSpec proposal: `openspec/changes/<change-id>/`
- ADR: `docs/architecture/<NNNN-title>.md`
- Closes #<issue-number>

## What changed

<!-- A flat list of the meaningful changes. File-level granularity is fine.
Skip if the diff is self-explanatory. -->

-
-

## Why

<!-- Motivation. The reason the change is worth merging.
For Implementation PRs, this is usually "see linked proposal." -->

## Notes for reviewers

<!-- Anything that isn't obvious from the diff:
- Tradeoffs you considered and rejected
- Areas you'd like a closer look at
- Follow-up work intentionally left out of this PR
-->

## Checklist

<!-- Tick what applies. Don't tick what isn't done. -->

- [ ] I read [CONTRIBUTING.md](../CONTRIBUTING.md)
- [ ] For non-trivial changes, an OpenSpec proposal exists and is linked above
- [ ] If this PR introduces an architectural decision, an ADR is included
- [ ] Affected documentation updated (`README.md`, `ROADMAP.md`, `docs/`, schema files)
- [ ] Code follows project conventions (`ruff` for Python, `prettier` for TS)
- [ ] No secrets, credentials, or large binaries committed
- [ ] PR title is in imperative mood (`add`, `fix`, `refactor`, not `added`/`fixing`)
